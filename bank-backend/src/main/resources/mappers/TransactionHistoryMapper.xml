<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bankscope.backend.mappers.TransactionHistoryMapper">

    <insert id="insertTransaction"
            useGeneratedKeys="true"
            keyColumn="transaction_id"
            keyProperty="transactionId">
        INSERT INTO `bank`.`transaction_history` (
            account_id,
            user_id,
            task_id,
            transaction_type,
            amount,
            balance_after,
            description,
            created_at,
            updated_at
        ) VALUES (
            #{transaction.accountId},
            #{transaction.userId},
            #{transaction.taskId},
            #{transaction.transactionType},
            #{transaction.amount},
            #{transaction.balanceAfter},
            #{transaction.description},
            IFNULL(#{transaction.createdAt}, NOW()),
            IFNULL(#{transaction.updatedAt}, NOW())
        )
    </insert>

</mapper>