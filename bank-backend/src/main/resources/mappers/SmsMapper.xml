<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bankscope.backend.mappers.SmsMapper">
    <insert id="saveAuthCode">
        INSERT INTO `bank`.`sms_auth` (phone_number, auth_code, is_verified, created_at, expires_at)
        VALUES (#{phoneNumber}, #{authCode}, false, NOW(), DATE_ADD(NOW(), INTERVAL 3 MINUTE))
        ON DUPLICATE KEY UPDATE
                             auth_code = #{authCode},
                             is_verified = false,
                             created_at = NOW(),
                             expires_at = DATE_ADD(NOW(), INTERVAL 3 MINUTE)
    </insert>

    <select id="getValidAuthCode" resultType="String">
        SELECT `auth_code`
        FROM `bank`.`sms_auth`
        WHERE phone_number = #{phoneNumber}
          AND expires_at >= NOW()
    </select>

    <update id="updateToVerified">
        UPDATE `bank`.`sms_auth`
        SET is_verified = true
        WHERE phone_number = #{phoneNumber}
    </update>

    <select id="isVerified" resultType="Boolean">
        SELECT is_verified
        FROM `bank`.`sms_auth`
        WHERE phone_number = #{phoneNumber}
          AND expires_at >= NOW()
    </select>

    <delete id="deleteAuthInfo">
        DELETE FROM `bank`.`sms_auth`
        WHERE phone_number = #{phoneNumber}
    </delete>
</mapper>